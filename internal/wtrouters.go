package wtutil

import "github.com/gin-gonic/gin"

func SetupRouter(router *gin.Engine) {

	// mainApiPath := "/api"

	// groupApiPath := mainApiPath + "/user"
	// userRouter := router.Group(groupApiPath)
	// userRouter.GET("/", userctr.GetUser)

}
